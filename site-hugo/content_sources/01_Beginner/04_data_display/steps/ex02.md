```py
"""
Step 2: Adding Key Metrics
Learning objective: Calculate and display metrics from DataFrame
"""

import streamlit as st
import pandas as pd

st.set_page_config(
    page_title="Data Display",
    page_icon="📊",
    layout="wide"
)

st.title("📊 Data Display Examples")
st.write("Learn different ways to display data in Streamlit")

data = {
    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Eve'],
    'Age': [25, 30, 35, 28, 32],
    'City': ['New York', 'San Francisco', 'London', 'Paris', 'Tokyo'],
    'Salary': [70000, 85000, 92000, 78000, 88000],
    'Department': ['IT', 'HR', 'Finance', 'IT', 'Marketing']
}

df = pd.DataFrame(data)

# Display metrics from the data
st.subheader("📈 Key Metrics")

col1, col2, col3, col4 = st.columns(4)

with col1:
    # len(df) gives the number of rows
    st.metric("Total Employees", len(df))

with col2:
    # Calculate average age
    avg_age = df['Age'].mean()
    st.metric("Average Age", f"{avg_age:.1f}")

with col3:
    # Calculate average salary
    avg_salary = df['Salary'].mean()
    st.metric("Avg Salary", f"${avg_salary:,.0f}")

with col4:
    # Count unique departments
    departments = df['Department'].nunique()
    st.metric("Departments", departments)

st.write(df)

```